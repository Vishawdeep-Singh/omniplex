specVersion: "v1beta"
serviceId: "omniplex"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "omniplex-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
